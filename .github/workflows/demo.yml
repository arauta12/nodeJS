name: Scheduled Workflow
on:
    push:
        branches:
            - main
jobs:
    test_schedule:
        runs-on: ubuntu-latest
        steps:
            - name: Step 1
              run: echo "DONE!"
